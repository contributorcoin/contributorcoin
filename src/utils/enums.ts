export const enum TransactionOptions {
  exchange = 'exchange',
  validation = 'validation',
  approval = 'approval',
  contribution = 'contribution',
  stake = 'stake'
}

export const enum GitProviders {
  github = 'github',
}
